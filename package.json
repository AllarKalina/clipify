{
  "name": "clipify",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "bun run --cwd apps/api dev",
    "start:api": "bun run --cwd apps/api start",
    "typecheck": "bun run --cwd apps/api typecheck && bun run --cwd packages/api-client typecheck && bun run --cwd apps/cli typecheck",
    "test": "bun run --cwd apps/api test && bun run --cwd packages/api-client test && bun run --cwd apps/cli test"
  }
}
